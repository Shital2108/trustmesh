# trustmesh
Deterministic zero-trust safety layer for AI-controlled IoT actuators. Gemini proposes commands; a hardcoded policy engine — no LLM in the loop — validates every command against physical safety bounds before execution, with a SHA-256 hash-chained audit log. Built for VoltHacks 2026.
